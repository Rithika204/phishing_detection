# phishing_detection
A machine learning project for detecting phishing URLs using Convolutional Neural Networks (CNN) and XGBoost. Includes preprocessing, feature extraction, model training, and automated execution via GitHub Actions.
